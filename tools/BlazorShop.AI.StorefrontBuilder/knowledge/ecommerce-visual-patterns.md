# Ecommerce Visual Patterns

The MVP pattern vocabulary covers shell navigation, home sections, product cards, category grids, product galleries, purchase panels, cart fallback, checkout fallback, account fallback, controls, and state components.
