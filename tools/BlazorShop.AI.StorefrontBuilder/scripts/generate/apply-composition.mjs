#!/usr/bin/env node
console.log("StorefrontBuilder composition applies shell, home, catalog, product, and fallback page files from generation-plan.yaml.");
console.log("Commerce commands remain bound through Starter slot/action contracts such as cart.add-line.");
