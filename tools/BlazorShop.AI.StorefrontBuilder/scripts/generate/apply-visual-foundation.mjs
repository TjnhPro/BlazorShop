#!/usr/bin/env node
import { mkdirSync, writeFileSync } from "node:fs";
import { dirname } from "node:path";

const projectRoot = readArg("--project-root") ?? "artifacts/storefront-builder/generated/BlazorShop.Storefront.GeneratedProof";
const output = `${projectRoot}/wwwroot/css/storefront-builder.generated.css`;

const css = `/* Generated by StorefrontBuilder. Source artifacts: metadata.yaml, asset-manifest.yaml, review-summary.md. Visual fidelity reference diff: not implemented. */
:root {
  --sfb-color-canvas: #fbfaf6;
  --sfb-color-surface: #ffffff;
  --sfb-color-ink: #1c2522;
  --sfb-color-muted: #68736f;
  --sfb-color-line: #d9dfd8;
  --sfb-color-brand: #0b6b57;
  --sfb-color-brand-strong: #084c3e;
  --sfb-color-accent: #c94c2f;
  --sfb-color-focus: #1e88e5;
  --sfb-font-sans: Inter, Arial, Helvetica, sans-serif;
  --sfb-text-xs: 0.75rem;
  --sfb-text-sm: 0.875rem;
  --sfb-text-md: 1rem;
  --sfb-text-lg: 1.25rem;
  --sfb-text-xl: 1.75rem;
  --sfb-text-hero: 2.5rem;
  --sfb-weight-normal: 400;
  --sfb-weight-medium: 600;
  --sfb-weight-bold: 700;
  --sfb-line-tight: 1.2;
  --sfb-line-normal: 1.5;
  --sfb-space-1: 0.25rem;
  --sfb-space-2: 0.5rem;
  --sfb-space-3: 0.75rem;
  --sfb-space-4: 1rem;
  --sfb-space-6: 1.5rem;
  --sfb-space-8: 2rem;
  --sfb-container: 72rem;
  --sfb-border-width: 1px;
  --sfb-radius-sm: 0.25rem;
  --sfb-radius-md: 0.5rem;
  --sfb-shadow-card: 0 10px 30px rgba(28, 37, 34, 0.08);
  --sfb-motion-fast: 120ms;
  --sfb-motion-standard: 180ms;
  --sfb-ease-standard: cubic-bezier(0.2, 0, 0, 1);
}

body {
  background: var(--sfb-color-canvas);
  color: var(--sfb-color-ink);
  font-family: var(--sfb-font-sans);
}

.starter-main {
  width: min(100% - 2rem, var(--sfb-container));
}

.starter-header,
.starter-footer {
  background: var(--sfb-color-surface);
  border-color: var(--sfb-color-line);
  box-shadow: var(--sfb-shadow-card);
}

.starter-brand {
  color: var(--sfb-color-brand-strong);
  font-weight: var(--sfb-weight-bold);
}

.starter-header a,
.starter-footer a {
  border-radius: var(--sfb-radius-sm);
  color: var(--sfb-color-ink);
  transition: color var(--sfb-motion-fast) var(--sfb-ease-standard), background-color var(--sfb-motion-fast) var(--sfb-ease-standard);
}

.starter-header a:hover,
.starter-header a:focus-visible {
  background: color-mix(in srgb, var(--sfb-color-brand) 12%, white);
  color: var(--sfb-color-brand-strong);
  outline: none;
}

.starter-product-grid {
  display: grid;
  gap: var(--sfb-space-6);
  grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
}

.starter-product-card,
.starter-section,
.starter-account-shell,
.starter-cart-lines,
.starter-checkout-step,
.starter-empty,
.starter-error,
.starter-loading {
  background: var(--sfb-color-surface);
  border: var(--sfb-border-width) solid var(--sfb-color-line);
  border-radius: var(--sfb-radius-md);
  box-shadow: var(--sfb-shadow-card);
}

.starter-product-card img,
.starter-product-gallery img {
  aspect-ratio: 1 / 1;
  object-fit: cover;
}

button,
.starter-button,
input,
select {
  border-radius: var(--sfb-radius-sm);
  font: inherit;
}

button,
.starter-button {
  background: var(--sfb-color-brand);
  border: var(--sfb-border-width) solid var(--sfb-color-brand);
  color: white;
  transition: background-color var(--sfb-motion-standard) var(--sfb-ease-standard);
}

button:hover,
.starter-button:hover {
  background: var(--sfb-color-brand-strong);
}

button:focus-visible,
.starter-button:focus-visible,
a:focus-visible,
input:focus-visible,
select:focus-visible {
  outline: 3px solid color-mix(in srgb, var(--sfb-color-focus) 70%, white);
  outline-offset: 2px;
}

.starter-empty,
.starter-error,
.starter-loading {
  color: var(--sfb-color-muted);
  padding: var(--sfb-space-6);
}

.sfb-shell-header {
  position: sticky;
  top: 0;
  z-index: 10;
}

.sfb-main-nav,
.sfb-mobile-nav,
.sfb-hero-actions,
.sfb-pagination {
  display: flex;
  flex-wrap: wrap;
  gap: var(--sfb-space-3);
}

.sfb-mobile-nav {
  display: none;
}

.sfb-cart-badge span {
  background: var(--sfb-color-accent);
  border-radius: 999px;
  color: white;
  display: inline-grid;
  font-size: var(--sfb-text-xs);
  min-inline-size: 1.4rem;
  place-items: center;
}

.sfb-hero,
.sfb-catalog-header,
.sfb-product-page,
.sfb-fallback-page {
  display: block;
  padding-block: var(--sfb-space-8);
}

.sfb-hero {
  font-size: var(--sfb-text-hero);
  line-height: var(--sfb-line-tight);
}

.sfb-featured-grid {
  display: grid;
  gap: var(--sfb-space-4);
}

.sfb-catalog-toolbar {
  background: var(--sfb-color-surface);
  border: var(--sfb-border-width) solid var(--sfb-color-line);
  border-radius: var(--sfb-radius-md);
  display: grid;
  gap: var(--sfb-space-4);
  grid-template-columns: minmax(0, 1fr);
  padding: var(--sfb-space-4);
}

.sfb-product-card {
  display: grid;
  gap: var(--sfb-space-3);
  padding: var(--sfb-space-4);
}

.sfb-product-gallery {
  aspect-ratio: 1 / 1;
  background: color-mix(in srgb, var(--sfb-color-line) 38%, white);
  border-radius: var(--sfb-radius-md);
  display: grid;
  overflow: hidden;
  place-items: center;
}

.sfb-product-purchase {
  display: grid;
  gap: var(--sfb-space-4);
}

.sfb-quantity-control {
  display: grid;
  gap: var(--sfb-space-2);
}

@media (max-width: 639px) {
  .starter-header,
  .starter-footer {
    align-items: flex-start;
    flex-direction: column;
  }

  .starter-header nav {
    width: 100%;
  }

  .starter-product-grid {
    grid-template-columns: 1fr;
  }

  .sfb-main-nav {
    display: none;
  }

  .sfb-mobile-nav {
    display: flex;
  }
}

@media (min-width: 640px) and (max-width: 1023px) {
  .starter-product-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (min-width: 1024px) {
  .starter-product-grid {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }
}
`;

mkdirSync(dirname(output), { recursive: true });
writeFileSync(output, css, "utf8");
console.log(`Generated visual foundation CSS at ${output}`);

function readArg(name) {
  const index = process.argv.indexOf(name);
  return index === -1 ? undefined : process.argv[index + 1];
}
