@page "/content/{Slug}"

<PageTitle>Content</PageTitle>
<h1>Content</h1>
<PlaceholderState
    Title="Content page"
    Message="SSR content reads will use the generated pages/content client."
    RenderOwner="SSR"
    HydrationMode="StarterHydrationMode.InitialSnapshot" />

@code {
    [Parameter]
    public string? Slug { get; set; }
}

