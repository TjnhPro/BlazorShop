@namespace BlazorShop.Storefront.Starter.Theme.Pages.Content
@using BlazorShop.Storefront.Presentation.Services.Content

<h1>@Context.Page.Title</h1>
<PlaceholderState
    Title="@Context.Page.Title"
    Message="SSR content is supplied by the shared Presentation content route."
    RenderOwner="SSR"
    HydrationMode="StarterHydrationMode.InitialSnapshot" />

@code {
    [Parameter, EditorRequired]
    public StorefrontContentPageContext Context { get; set; } = default!;
}
