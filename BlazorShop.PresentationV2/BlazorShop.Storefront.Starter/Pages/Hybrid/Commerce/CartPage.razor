@using BlazorShop.Storefront.Presentation.Services.Cart

<PageTitle>Cart</PageTitle>
<h1>Cart</h1>
<PlaceholderState
    Title="Cart shell"
    Message="Cart browser commands call same-origin /api/cart/* BFF endpoints."
    RenderOwner="Hybrid"
    HydrationMode="StarterHydrationMode.InitialSnapshot" />
<CartLineList />

@code {
    [Parameter]
    public StorefrontCartPageContext Context { get; set; } = new(null, [], "/checkout", "/new-releases", "/deals");
}
