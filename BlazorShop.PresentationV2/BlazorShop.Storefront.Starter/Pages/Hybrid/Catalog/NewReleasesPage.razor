@namespace BlazorShop.Storefront.Starter.Theme.Pages.Catalog

<h1>New releases</h1>
<PlaceholderState
    Title="New releases shell"
    Message="SSR snapshot supplies newly published product listing data."
    RenderOwner="Hybrid"
    HydrationMode="StarterHydrationMode.InitialSnapshot" />

@code {
    [Parameter, EditorRequired]
    public StorefrontCatalogProductsPageContext Context { get; set; } = default!;
}
