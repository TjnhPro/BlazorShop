<div class="starter-skeleton" aria-hidden="true">
    @for (var index = 0; index < Lines; index++)
    {
        <span></span>
    }
</div>

@code {
    [Parameter]
    public int Lines { get; set; } = 3;
}
