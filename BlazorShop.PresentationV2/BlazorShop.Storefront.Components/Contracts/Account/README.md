# Account Contracts

Browser-safe account contracts live here when they are independent of one storefront visual template. Account auth/session policy and local endpoint route names remain host-owned.

These are stable presentation contracts, not public HTTP API DTOs, and they must not import Headless, Browser, or Features namespaces.
