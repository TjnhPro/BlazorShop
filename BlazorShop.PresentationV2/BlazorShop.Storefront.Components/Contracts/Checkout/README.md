# Checkout Contracts

Browser-safe checkout contracts live here when they are independent of one storefront visual template. Checkout business rules remain in Commerce Node and local BFF orchestration remains host-owned.

These are stable presentation contracts, not public HTTP API DTOs, and they must not import Headless, Browser, or Features namespaces.
