# Deals Contracts

Presentation-only deals contracts live here when they are independent of one storefront visual template. They are stable presentation contracts, not public HTTP API DTOs, and they must not import Headless, Browser, or Features namespaces.
