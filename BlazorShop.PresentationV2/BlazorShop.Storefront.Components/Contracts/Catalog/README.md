# Catalog Contracts

Presentation-only catalog contracts live here after migration. They must not define Storefront V2 visual markup, route names, endpoint paths, backend DTOs, or store-specific layout assumptions.

These are stable presentation contracts, not public HTTP API DTOs, and they must not import Headless, Browser, or Features namespaces.
