# Cart Contracts

Browser-safe cart contracts live here when they are independent of one storefront visual template. Host-owned local endpoint routes stay outside this folder.

These are stable presentation contracts, not public HTTP API DTOs, and they must not import Headless, Browser, or Features namespaces.
