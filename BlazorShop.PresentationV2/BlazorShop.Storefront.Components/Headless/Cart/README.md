# Cart Headless Behavior

Headless cart behavior lives here after migration. It may expose loading, mutation, alert, and event state over host-supplied actions, but it must not hardcode `/api/cart` paths or V2 layout classes.

